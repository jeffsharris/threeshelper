# J2A1 V3 Distillation Recovery Execution Surface

Status: outcome-free execution-surface implementation and readiness only.
No authorization, phase lock, marker, materialization, collector, teacher
query, label, game, optimizer step, checkpoint, family/mechanism/fidelity
read, or policy outcome is authorized in this construction turn.

## 1. Immutable predecessors

The authoritative recovery preflight is
`READY_J2A1_V3_DISTILLATION_RECOVERY_PREFLIGHT`. Its source identities are:

- charter
  `4638ffbcc67806742a1683d4aeec39a9669055d2051c18768a5ea5cd68aa216e`;
- runner
  `c4bbb5b79a6a8df17e4b97663f49e0a5a2db06fe8d7b5e8bb483aa67ff3c8c43`;
- tests
  `577dfe0fa434cbb8d95e6e780d5396e692ae63679e57a6ce64cf4214ab9b7705`.

All nine files in
`threes_rl/runs/forensics/`
`j2a1_distillation_fidelity_recovery_readiness_v3`
are binding:

- test evidence file/payload
  `a45fe7b7e1eaf564e55c91ab9b0e25488fd958020fb9dc3138077df7294d808b` /
  `0ad095a211fb69eb12892faa9f9e2047317b3cd3367a933dba476e1797766d3e`;
- input bindings
  `c6678f3bae0b7d85c4caddf09e44acd35932050a28b2da4afe67fe940a8531cb` /
  `5a87f7d02a74f6493bc15683fa7f5a410b3fa89dd57866e7ea82a2820f51a275`;
- V2 integrity
  `b4527df82c154e8831965050ef5eb6a5a124d5ed6e383f5402254d50fb556d58` /
  `8c5a2e5c8de7f0687084957cccc42e04e6dcf5da17127a4aa36b547698be0059`;
- recovery authority
  `ca6f1bd99a9c6d3654e4af04227a1aad0f1d4d012f0eb3cedea1f3e405523691` /
  `13ca1ac368dbf679d39ba6d563deaa759dd8ed167f251641e95f062820c72a4b`;
- wall projection
  `0f949e4e84495354f567ae91a8e848b113dbe7625bcc19b302a0c77351eb0f99` /
  `c544b2e9285a60be7d654e34235a4ff211c58fc8b2d8e3b9d5aa37bbf6acda72`;
- schema
  `bd9169c544cf9d1f212a4995c0727e6ca101bf18260ae603407f4062f0d35c4b` /
  `29f7a421c640a927f36da9c2947399b518e6da5cb24b8945e65e6322801dac3c`;
- readiness lock
  `0f8d4f916b9672dfbe2844595952053c81f153dd6b2d01bd8c30486204bb0153` /
  `d6bb27e29bda678afdbb82d5cbfa342338c2dcc8ff053fe09d159a3432879ca6`;
- readiness result
  `23199ead16dce7ac87ea7d955bba5c913be632f624fa8771fc01a07669ab33ae` /
  `66ef7008eb71e3ebf088d908170f510e823a884adbb07e35daeae35b17a8cc56`;
- retention
  `26e07603590d39e5402e6f95f35efc94933c83d68931e42a0b1de4b9f49c3246` /
  `2c4072d9a6771b89728336b57062c4d0b1ef06de77248241a05c35dabde9ee4a`.

The spent V2 source, readiness, authorization, execution terminal, evidence,
retention, reservation, consumption, active manifest, attempt ledger,
completion ledger, and 3,048 retained root-byte identities remain binding
exactly as recorded by that preflight. V2 is never edited, resumed, or
reinterpreted.

## 2. Separate surface and namespaces

The new source files are:

- `threes_rl/`
  `J2A1_DISTILLATION_FIDELITY_V3_RECOVERY_EXECUTION_SURFACE_CHARTER.md`;
- `threes_rl/`
  `j2a1_distillation_fidelity_recovery_execution_surface_v3.py`;
- `tests/`
  `test_rl_j2a1_distillation_fidelity_recovery_execution_surface_v3.py`.

The zero-work readiness namespace is
`threes_rl/runs/forensics/`
`j2a1_distillation_fidelity_recovery_execution_surface_readiness_v3`.
The future authorization namespace is
`threes_rl/runs/forensics/`
`j2a1_distillation_fidelity_recovery_authorization_v3`.
The future execution namespace remains the preflight-bound
`threes_rl/runs/forensics/`
`j2a1_distillation_fidelity_recovery_v3`.
Both future namespaces must remain absent in this turn.

The public CLI is exactly:

1. `audit-zero-work`
2. `write-test-evidence`
3. `prepare-readiness`
4. `seal-phase-lock`
5. `open`
6. `materialize`
7. `execute`

Only the first three commands are invoked in this turn.

## 3. Authorization and phase order

A future research-lead authorization is a separately named create-once JSON
artifact generated only by the frozen `authorization_payload(...)` helper.
It binds the complete execution-surface readiness package, the complete
recovery-preflight package, V2 terminal/evidence/retention and stream
authority, a fresh byte-only 3,048-root audit identity, the exact future
execution root, scientific mode, jobs=1, and the four phase commands.
Readiness never creates it.

`seal-phase-lock` requires a fresh execution root, exact authorization,
current source/readiness/preflight/V2 identities, nice at least 10, one heavy
job, services/top-three/human opacity, more than 100 GiB free with more than
120 GiB the target, and zero scientific work.

`open` requires the immutable lock and creates one content-blind marker.
`materialize` requires the lock and marker and copies no scientific body. It
materializes exactly the 11,288 preflight-frozen unfinished metadata rows in
canonical authority order and an exact 3,048-row completed-reference identity.
`execute` requires and rehashes every predecessor from disk.
Readiness itself also rehashes the current bytes of all 3,048 completed root
files against those references without deserializing any root body.

No phase may be skipped, repeated under another marker, or activated by a
fixture/non-scientific predecessor.

## 4. Existing stream authority

The V2 reservation and consumption are the sole authority for all 63,488
streams. V3 writes no reservation or consumption artifact. After
materialization and before work, it writes one create-once
`STREAM_AUTHORITY_REUSE.json` that binds the exact V2 reservation and
consumption file/payload identities, full stream-set identity, marker, lock,
materialized authority, and zero new reservations/consumptions.

Only the 11,288 unfinished rows may be assigned. The 3,048 completed roots
are recognized through authenticated completion metadata and current byte
hashes and are never queried again. Completed rows are the exact
`teacher_behavior_cloning` prefix `0..3047`. Remaining BC rows and all
validation rows retain original stage-local row indices, canonical ordering,
stream identities, and shard `row_index mod 8`. There is no work stealing,
replacement, filtering, rebalancing, or duplicate stream use.

Readiness and phase setup never deserialize a V2 root body. A future execute
may deserialize any root only after a create-once union seal proves all
14,336 roots complete and byte-authenticated.

## 5. Recovery collection and restart

One top-level jobs=1 process at nice 10 owns the phase. It launches exactly
eight fixed single-thread teacher collectors. The existing exact incumbent
binding and inherited V2 collection primitive remain byte-bound.

The V3 attempt and completion ledgers are append-only, self-hashed, and
contract-bound. They contain only the 11,288 unfinished authorities. A
completed V2 root cannot enter either V3 ledger. Every V3 attempt opens once
in its original shard and stream, closes once against one immutable root
blob, and may never be retried after completion.

Ownership is create-once. A live, unrelated, wrong-marker, wrong-lock,
wrong-manifest, wrong-command, wrong-runner, or malformed owner fails closed.
A verifiably dead same-contract owner may be reclaimed only by an immutable
record binding old and new owner identities, process-death evidence, last
authenticated commit head, and zero concurrent writer.
Reclaim scans the complete commit hash chain once and verifies every immutable
file identity plus every append-only journal prefix referenced by that chain.
Caller-supplied commit digests are never trusted as recovery evidence.

After a crash, committed roots remain complete. Only an uncommitted root
block may be deterministically replayed in its original shard. The abandoned
unit receives its frozen conservative wall charge. Canonical merge is original
authority order, independent of worker completion order.

## 6. Wall time and resources

The 72-hour cap uses cumulative top-level live execution wall time:

`32608.277662038803 V2 seconds + authenticated V3 live wall seconds`.

V2 aggregate worker time `259763.24813699722` seconds and V3 per-worker,
attempt, and CPU seconds are descriptive only. They never enter the cap.

The V3 wall ledger is append-only and hash-chained. It records process
segments, unit kinds, monotonic boundary heartbeats, finishes, and dead-owner
abandonment. Dead-process/adjudication/reboot downtime is never charged.
Collection, distillation, and fidelity use distinct typed wall segments; a
heartbeat carrying the wrong segment type is an integrity failure.
Uncommitted work is charged once by the frozen ceiling:

- teacher eight-root block:
  `0.1316514358320273 * 5000 = 658.2571791601365` seconds;
- distillation minibatch: `60.0` seconds;
- student fidelity pair: `5.0` seconds.

Every scientific boundary verifies cumulative wall below 72 hours, output
below 24 GiB, free disk above 100 GiB, nice/process/thread/service/top-three
guards, and ownership. The accepted recovery point and conservative Stage A
projections remain `42.602824125381694h` and `50.989066430196644h`.
Output accounting performs one namespace scan per process start, targeted
file-size updates at recovery boundaries, and one full terminal
reconciliation; it never walks the growing tree at every root block.

Storage inherits the exact 20.41377067565918-GiB maximum-shape projection,
including historical roots, only current-stage recovery material, one
ephemeral BC batch, fidelity pairs, ledgers, commits, terminal, retention,
and 25% margin. Recovery metadata adds a frozen bounded allowance and the
combined projection must remain below 24 GiB.
A 16-MiB terminal-finalization allowance is reserved inside that recovery
allowance and checked before the authoritative terminal is written.

## 7. Union and inherited scientific gates

Stage B is structurally inaccessible until
`J2A1_V3_RECOVERY_UNION_COMPLETE.json` proves:

- 14,336 exact original roots and ancestries;
- 3,048 exact V2 completions plus 11,288 exact V3 completions;
- one completion and unique byte identity per root;
- canonical authority-order merge;
- 63,488 exact original streams with no duplicate consumption;
- zero replacement, filtering, or missing authority;
- current V2 source/terminal/evidence/retention hashes unchanged.

Only after this seal may execute load root bodies. It then invokes the exact
hash-bound inherited feature-family, eight-epoch root-equal BC, mechanism,
and 6,144-pair sustained closed-loop fidelity implementations and thresholds.
No threshold, architecture, optimizer, ordering, seed, weighting, bootstrap,
or decision rule changes.

After the scientific gates close, one final operational guard is sealed.
Only a READY scientific result plus that exact guard may create the
checkpoint-authority artifact. Until then the checkpoint is non-authoritative;
an operational KILL or scientific HOLD quarantines it.

A clean family, mechanism, base-rate, or fidelity miss seals the inherited
HOLD and quarantines every checkpoint. Integrity, source, authority, stream,
chronology, numerical, metric-authentication, terminal, or retention failure
seals KILL. Operational/resource failure also seals KILL for this one-shot
recovery, with no scientific reinterpretation or in-place retry. PASS permits
only a separate PPO
execution-surface review.

PPO, development, confirmation, promotion, alternate branches, human
training, human-session content, incumbent mutation, and dashboard mutation
remain forbidden.

## 8. Terminal and retention

Terminal precedence is:

1. integrity KILL;
2. operational KILL;
3. family/mechanism/base-rate/fidelity HOLD;
4. READY for separate PPO surface review.

Terminal evidence and retention are sealed before the authoritative terminal,
which is written last. A crash after evidence or retention but before the
terminal is idempotently finalized from those exact bytes; an invalid existing
artifact is never overwritten.
The terminal binds V2 and V3 attempts separately, cumulative wall time,
worker seconds as descriptive only, union identity, checkpoint quarantine or
authority, current owner chain, stream-authority reuse, and all zero forbidden
work counters.

## 9. Readiness artifacts and tests

The readiness namespace contains exactly:

1. `J2A1_V3_RECOVERY_SURFACE_TEST_EVIDENCE.json`;
2. `J2A1_V3_RECOVERY_SURFACE_INPUT_BINDINGS.json`;
3. `J2A1_V3_RECOVERY_SURFACE_AUTHORITY_AUDIT.json`;
4. `J2A1_V3_RECOVERY_SURFACE_SCHEMA.json`;
5. `J2A1_V3_RECOVERY_SURFACE_PROJECTION.json`;
6. `J2A1_V3_RECOVERY_SURFACE_STATE_MACHINE_AUDIT.json`;
7. `J2A1_V3_RECOVERY_SURFACE_READINESS_LOCK.json`;
8. `J2A1_V3_RECOVERY_SURFACE_READINESS_RESULT.json`;
9. `J2A1_V3_RECOVERY_SURFACE_RETENTION.json`.

Focused tests cover cumulative wall accounting, eight-worker descriptive
accounting, V2 completion import, exact unfinished authority, no completed
root query, no second consumption, fixed shard/canonical merge, live and dead
owner cases, recursive commit-prefix authentication, crash/restart identity,
typed wall transitions, stage-B completeness, phase chronology,
authorization/marker/materialization tampering, storage and operational
guards, terminal finalization, terminal precedence, checkpoint quarantine,
create-once behavior, and future namespace absence.

## 10. Readiness decisions

The only readiness decisions are:

- `READY_J2A1_V3_RECOVERY_EXECUTION_SURFACE`;
- `HOLD_J2A1_V3_RECOVERY_EXECUTION_SURFACE`;
- `KILL_J2A1_V3_RECOVERY_EXECUTION_SURFACE_INTEGRITY`.

READY authorizes only research-lead review of a future authorization artifact.
It does not authorize a phase lock or execution.

`CONTINUE=research-lead review of the V3 execution surface`;
`HOLD=all collectors, inherited scientific stages, PPO, development,
confirmation, promotion, alternate branches, and human training`;
`KILL=historical locks and V1 execution reuse`;
`PROMOTE=false`.
