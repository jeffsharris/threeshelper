# J1d V2 Exact Metric-Authentication Amendment

Status: outcome-free readiness correction only. J1d training, development,
confirmation, promotion, and every other scientific phase remain held.

## 1. Historical boundary

The J1d V1 charter, runner, tests, prospective manifest, stream authority,
root-cause evidence, and test evidence are immutable pre-correction evidence.
The V1 test evidence has file SHA-256
`d2957b138a734dd3185a1b5f09eba782a7632027cdd06b0cb0ebd4042e7f064c`
and canonical payload SHA-256
`4e0a6068289472478fddf199c86a803b1dae53b6f55fbb5e32bab0566ab0b3da`.
No V1 readiness terminal or execution phase was sealed. The future execution
root `threes_rl/runs/forensics/j1d_execution_v1` remains absent.

J1c remains permanently `KILL_J1C_TRAINING_INTEGRITY`. Its checkpoint,
metrics, roots, transitions, streams, optimizer state, and episode bodies are
forbidden. All earlier locks and scientific semantics remain unchanged.

## 2. Sole repair

For each authenticated round, the ordered per-root metric rows are the sole
authority for published root-equal aggregates. One canonical function derives:

- ordered root log scores;
- legal entropy;
- value MSE and zero-value MSE;
- the three auxiliary Brier scores; and
- the three prevalence-baseline Brier scores.

The writer and verifier must call that same function. The published projection
must equal the canonical projection exactly as JSON-native Python values, and
its SHA-256 must equal the canonical aggregate SHA-256. A one-ULP mutation must
fail even if every exposed authentication hash is recomputed. The inherited
`1e-12` parent scientific tolerance remains recorded and unchanged; it cannot
substitute for exact publication authentication.

The mechanism regression must compute, rather than inject, both historical
paths from finite adversarial transition data with intentionally unequal root
lengths:

1. the legacy direct global root-equal weighted reduction; and
2. the canonical manifest-order mean of authenticated per-root reductions.

At least one affected metric must diverge by more than `1e-12`. The historical
J1c discrepancy magnitudes remain separate structural attribution evidence.

No model, seed, optimizer, PPO objective, schedule, reward, auxiliary,
mask, checkpoint, sanity threshold, resource cap, or scientific decision rule
changes.

## 3. Prospective authority

V2 uses a new readiness namespace:
`threes_rl/runs/forensics/j1d_metric_authentication_readiness_v2`.
It may reproduce the still-unused J1d prospective block under a new V2
root-commitment identity:

- logical `213000049152..213000065535`;
- deck `214000049152..214000065535`;
- slot `215000049152..215000065535`;
- candidate `216000049152..216000065535`.

All declared J1, J1b, and J1c ranges remain spent. V2 must bind the immutable V1
evidence identities, prove zero collision, and keep the future execution root
absent. The public dispatcher remains exactly `seal-phase-lock`, `open`,
`materialize`, and `execute`, but none may be invoked during this readiness
turn.

## 4. Evidence and decision

Before a V2 readiness terminal:

- run `py_compile`, focused V2 tests, immutable parent suites, and applicable
  non-scientific regressions under nice 10;
- run a real-shape 64-round, 16,384-root synthetic authentication fixture;
- verify create-once behavior, hash-recomputed one-ULP tamper rejection,
  computed legacy/canonical divergence, crash resume, source/stream identities,
  services, disk, one-heavy-job status, and zero scientific work;
- preserve active human sessions as opaque and unread.

Seal exactly one of:

- `READY_J1D_V2_METRIC_AUTHENTICATION_PREFLIGHT`;
- `HOLD_J1D_V2_METRIC_AUTHENTICATION_PREFLIGHT`; or
- `KILL_J1D_V2_METRIC_AUTHENTICATION_PREFLIGHT_INTEGRITY`.

Even READY authorizes no marker, reservation, stream consumption, game,
optimizer step, checkpoint, development, confirmation, evaluation, or
promotion.
