# K1 Compiled Exact Kernel Charter Amendment A4

Status: frozen before implementation and before any fresh K1 game, compiled
fresh-gate library, timing, depth-3 result, policy outcome, or score inspection.

Date: 2026-07-26

## Trigger

The first K1 zero-work preflight attempted to reserve stream bases
69,000,000,000 through 72,000,000,000. Its sandboxed process audit failed
before a lock could be promoted. The escalated retry then correctly found those
same IDs in the preserved failed staging artifact and failed closed. No game,
stream, compilation, timing, depth-3 result, policy outcome, or score was
produced.

The failed staging directories are immutable evidence and are not deleted or
excluded from the historical collision union. The 69B through 72B namespaces
are therefore treated as spent zero-work reservations.

## Sole Scientific-Contract Change

Replace the K1 fresh engineering stream bases exactly once:

- logical/reset seed: `73_000_000_000`
- deck stream: `74_000_000_000`
- slot stream: `75_000_000_000`
- policy stream: `76_000_000_000`

The family offset and game-index formula remain unchanged. A complete
outcome-free historical scan performed before this amendment found no use of
the four new base IDs.

## Preserved Contract

All K1 model, native source, compiler, flags, exactness semantics, family
slate, game/root/state counts, deterministic extraction, partitions, timing
schedule, runtime thresholds, one-shot sealing, resource bounds, and
non-promotable decision rules remain unchanged.

The K1 runner, focused tests, broader regressions, test-evidence artifact, and
zero-work preflight must be freshly hashed after implementing this amendment.
The earlier test evidence and both failed staging directories remain preserved
as superseded engineering evidence.
